package com.keevo.commerce.sale.domain.model;

/**
 * PaymentMode — enumeration of supported payment methods.
 * Pure Java — no framework imports.
 */
public enum PaymentMode {
    CASH,
    MOBILE_MONEY
}
