package com.keevo.identity.auth.domain.model;

/**
 * Role — User roles in the Keevo system.
 *
 * <p>Pure Java enum — NO framework imports.
 */
public enum Role {
    OWNER,
    EMPLOYEE,
    SUPER_ADMIN
}
