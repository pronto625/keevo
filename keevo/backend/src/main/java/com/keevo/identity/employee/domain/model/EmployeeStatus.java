package com.keevo.identity.employee.domain.model;

/**
 * EmployeeStatus — Employee lifecycle states.
 *
 * <p>Pure Java enum — NO framework imports.
 * Story 3.5 — AC7 (deactivation), AC8 (list view).
 */
public enum EmployeeStatus {
    ACTIVE,
    INACTIVE
}
