# Deferred Work

## Deferred from: code review of v1s-15-3-red-tests-missing-classes (2026-07-23)

- **`@PreAuthorize`/RBAC jamais exercé dans `CategoryControllerTest`** : `CategoryController` porte `@PreAuthorize("hasAnyRole('OWNER','EMPLOYEE')")` (classe) et `@PreAuthorize("hasRole('OWNER')")` (DELETE), mais `standaloneSetup` ne monte aucune chaîne de sécurité. Gap de convention project-wide (40/41 tests de controller ne testent pas la sécurité, y compris `EmployeeControllerTest` mirror explicite de ce test) — candidat pour une story RBAC/security-testing dédiée.
- **`DELETE /categories/{id}` not-found jamais caractérisé** : `categoryRepository.deactivate(id)` lève `IllegalArgumentException` (`JpaCategoryRepository.java:120-125`) — même famille de gap 500 que D2, mais non identifié par l'investigation D2 de cette story (qui n'a listé que les 3 use cases Create/Toggle/Rename). Ajouter un test miroir des 3 déjà présents.
- **Paramètre UUID malformé jamais testé** sur aucun endpoint `CategoryController` — 400 via `MethodArgumentTypeMismatchException`, handler déjà présent dans `GlobalExceptionHandler`, juste jamais exercé.
- **`StockMovementRepositoryAdapterTest` — mapping `variantId` non-null jamais vérifié** (seul le cas `null` est testé).
- **`EmployeeRepositoryAdapterTest` — `findByUserId` : branche `Optional.empty()` non testée** (seul le cas "trouvé" est couvert).
- **`EmployeeRepositoryAdapterTest` — `EmployeeStatus.valueOf(status)` sur donnée corrompue** : lève une `IllegalArgumentException` non gérée en cas de statut inconnu/corrompu en base, aucun test ne couvre ce cas.
- **`FailoverWhatsAppAdapterTest` — combinaisons non testées** : "token Wassender blank/null ET Twilio échoue aussi" et "Twilio non configuré pendant un failover".
- **`sanitizePhone` — branche "préfixer +" jamais exercée** dans `TwilioWhatsAppAdapterTest` ni `FailoverWhatsAppAdapterTest` (tous les numéros de test commencent déjà par `+`).
- **`JdbcProfitabilityRepositoryTest.findDailyMarginLast7` — `RowMapper` jamais invoqué/asserté** (seule la fenêtre de dates est vérifiée).
- **`ReportHistoryServiceTest` — `resendReport` avec téléphone propriétaire null/blank jamais testé.**
- **`ReportHistoryServiceTest` — renvoi d'un rapport déjà `SENT`/`FAILED` jamais testé**, alors que `ReportHistoryService.java:65` documente explicitement ce comportement comme intentionnel (pas de garde). Règle métier documentée avec zéro couverture.
- **`EmployeeRepositoryAdapterTest` — réflexion sur le champ privé `createdAt` de `JpaBaseEntity`** pour simuler `@PrePersist` : couplage fragile, casse de façon opaque sur un refactor sans rapport avec cette story.

## Deferred from: code review of v1s-14-12-forgot-password-otp-whatsapp (2026-07-23)

- **`@Transactional` englobe `whatsAppPort.sendOtp()` — pool connexions DB épuisable** : `RequestPasswordResetService.java:58,91`. Network call synchrone 5-30s dans transaction = connexion DB immobilisée. Acceptable V1 (faible volume, single instance), à refactorer avec TransactionTemplate ou `@TransactionalEvent` avant scale.
- **Race condition `attempts` — pas de `@Version` optimistic locking** : `ResetPasswordService.java:89-90`. Read-modify-write non atomique sur `attempts` du token. Exploitation requiert 5 requêtes exactement simultanées ; token TTL 10min + lockout soft rendent l'impact limité.
- **Index DB manquant sur `consumed_at` + pas de job purge tokens** : `V6__password_reset_tokens.sql`. Requêtes `consumed_at IS NULL` sans index dédié, table grossit sans nettoyage. À traiter dans story de suivi (cleanup job + index partiel).
- **Test `shouldBlockSixthRequestWithinOneHour` ne teste rien** : `PasswordResetRateLimiterTest.java`. Fait 5 appels sur 5 téléphones différents, ne vérifie jamais la limite horaire du MÊME phone. À corriger dans story de suivi tests.
- **ResetPasswordPage — pas de fallback si `phoneNumber` absent (deep link)** : `reset_password_page.dart` + `app_router.dart`. Deep link direct sans `extra` → page cassée ("au ."). UX edge case rare.
- **PII `phoneNumber` dans `PasswordResetRequestedEvent` object** : `PasswordResetRequestedEvent.java`. Phone en champ du record mais audit listener ne le sérialise PAS (Map.of("userId", ...)). Spring events in-memory sync par défaut — pas de leak réel en config actuelle. À surveiller si Sentry/Micrometer activés avec event tracing.
- **OTP inputs séparés non implémentés** : `reset_password_page.dart`. Spec AC7 dit "6 chiffres, auto-focus, inputs séparés", code fait 1 seul TextFormField maxLength 6. Fonctionnel OK, UX dégradée vs spec littérale.
- **`PasswordResetRateLimiter` — fuite mémoire ConcurrentHashMap sans eviction** : `PasswordResetRateLimiter.java:21-24`. `perMinute`/`perHour` sans TTL/éviction. Chaque phone unique reste en mémoire pour la durée de vie JVM. Acceptable V1 (volume borné), à traiter avec scheduled cleanup ou Caffeine avant scale.

## Deferred from: code review of v1s-14-10-notif-owner-employee-actions (2026-07-22)

- **Logique d'extraction acteur dupliquée** : `ProductController`, `StockOperationService`, `QuickAddProductService` répliquent chacun le même pattern « premier `ROLE_` authority + `AuthDetails.firstName` avec fallback UUID » (le 4e site, dans `StockController`, était du code mort et a été supprimé lors du patch de code review). `AuthDetails`, introduit dans ce même diff comme infra partagée, serait le point de centralisation naturel. Refactor non bloquant. Fichiers : `ProductController.java`, `StockOperationService.java`, `QuickAddProductService.java`.
- **Fallback UUID brut si `firstName` absent du JWT** : les sites d'extraction affichent l'UUID de l'acteur dans le texte de notification quand `AuthDetails.firstName()` est null, sans test couvrant ce chemin. Dégradation cosmétique uniquement (l'infra `firstName` claim, pré-existante depuis Story 7.1, est généralement peuplée). Fichiers : `ProductController.java`, `StockOperationService.java`.
- **`batchWindows` (ConcurrentHashMap) jamais évincée** : les entrées par `tenantId:actorId` s'accumulent pour la durée de vie de la JVM sans TTL/éviction. Croissance mémoire lente et bornée par la cardinalité tenant×employé (faible en pratique pour ce SMB) ; le Dev Notes de la story acte déjà le caractère mono-instance MVP → Redis en Phase 2. Fichiers : `ProductCreationNotificationListener.java`, `StockModificationNotificationListener.java`.
- **`QuickAddProductService` ne résout pas `storeName`** (toujours `null` dans `ProductCreatedEvent` pour les produits ajoutés via inventaire) alors que `session.getStoreId()` est connu — gap de qualité notification identique en nature au fix appliqué à `StockOperationService`/`ProductController`, découvert après la clôture du triage initial de cette story. Fichier : `QuickAddProductService.java` (~ligne 96, event `ProductCreatedEvent` publié avec `storeName=null`).

**Note :** un finding initialement classé « defer » (`extractRole()` lève `FORBIDDEN`) s'est révélé être une régression réelle (cassait `ProductControllerTest`) et a été corrigé, pas déféré — voir Review Findings de la story.

## Deferred from: code review of v1s-14-9-fr20-banners-session-revoke-ui (2026-07-22)

- **`subscriptionInfoProvider` (dérivation `trialExpired`) toujours pas invalidé au login/logout/changement de tenant** : contrairement à `tenantStatusClaimProvider` (patché, voir Review Findings de cette story), `subscriptionInfoProvider` garde sa staleness pré-existante — sur un device partagé (POS multi-employé), la bannière "plan Gratuit" peut refléter le tenant précédemment connecté jusqu'au premier autre événement qui invalide ce provider. Non corrigé dans cette story : `ref.invalidate(subscriptionInfoProvider)` force une reconstruction immédiate (FutureProvider non-autoDispose) qui déclenche un vrai appel réseau à chaque transition d'auth — testé, casse `register_page_test.dart` (Timer pendant, Dio non mocké dans ce test). Nécessite soit `autoDispose` sur `subscriptionInfoProvider` (impact sur `subscription_page.dart`, à vérifier), soit une invalidation conditionnelle/differée. Fichiers : `keevo/app/lib/features/settings/presentation/provider/subscription_info_provider.dart`, points d'invalidation dans `auth_provider.dart`/`settings_page.dart`.
- **`SuspensionBanner` variante `trialExpired` illisible (contraste ~1.6:1)** : texte blanc (`Colors.white`) sur fond ambre `AppTheme.warning` (`0xFFFCC419`) — bien en-dessous du minimum WCAG AA (4.5:1 texte normal, 3:1 UI/large). Le widget `suspension_banner.dart` existait déjà (non touché par le diff de cette story), mais n'était monté nulle part avant Story 14.9 — le défaut devient donc visible/live pour la première fois. Fix trivial (texte foncé sur fond ambre, ou fond ambre plus saturé), mais hors du diff strict de cette story. Fichier : `keevo/app/lib/features/settings/presentation/widget/suspension_banner.dart:47-56`.
- **Pas de priorité/empilement entre bannières** : `SuspensionBanner` + `SyncWarningBanner` + `OfflineGateBanner` peuvent s'afficher simultanément (ex. tenant suspendu + hors-ligne) sans logique de priorité, empilant jusqu'à 3 bannières pleine largeur. Pattern d'empilement pré-existant (Sync+Offline empilaient déjà avant cette story), simplement étendu par le montage de la 3ème bannière. Fichier : `keevo/app/lib/core/scaffold/main_shell.dart:279-282`.

## Deferred from: code review of v1s-14-8-api-contract-breaks (2026-07-22)

- **`ApiResponseWrapper.paginated()` sans validation d'entrée** (page/size/totalElements/totalPages négatifs ou incohérents) : aucun appelant aujourd'hui (Décision D3, capacité additive pure). À revisiter quand Story 15.6 câble de vrais controllers et définit le contrat de validation. Fichier : `ApiResponseWrapper.java:48`.
- **`ReportsDeltaProvider` plafonné à 1000 lignes sans pagination par curseur** : au-delà de 1000 rapports créés entre deux pulls, les lignes excédentaires sont silencieusement perdues (le watermark avance à `now()` sans égard à la troncature). Pattern pré-existant mirroré à l'identique depuis `DayClosureDeltaProvider.java` (fichier non touché par cette story, même défaut). Fichier : `ReportsDeltaProvider.java:41`.
- **`ReportsDeltaProvider.bool()` risque `ClassCastException`** si le driver JDBC retourne autre chose qu'un `Boolean`/`Number` pour `is_automatic`. Pattern identique à `DayClosureDeltaProvider.bool()`, pré-existant. Fichier : `ReportsDeltaProvider.java:61`.
- **`ReportsDeltaProvider.num()` tronque via `intValue()`** : perte de précision théorique au-delà de `Integer.MAX_VALUE`. Pattern identique à `DayClosureDeltaProvider.num()`, pré-existant ; colonnes DB `total_revenue`/`total_sales` sont de type `INTEGER`, donc aucun impact réel actuellement. Fichier : `ReportsDeltaProvider.java:64`.

## Deferred from: code review of v1s-13-6 (2026-07-22)

- **Code mort validation OWNER (ventes brouillon)** : `PendingSalesPage`, `PendingSaleDetailPage`, `ValidateSaleService` (branche OWNER manuel), `SaleValidationCascadeService`, `pendingSalesCountProvider`, la variante ambre de `SaleSuccessPage` (`saleStatus == 'PENDING_VALIDATION'`) sont tous fonctionnels mais **plus jamais atteints** depuis le 2026-05-03 (commit `30bdd07`, redesign du flux brouillon vers inline stock + auto-promotion). Aucun chemin client ne produit de vente `PENDING_VALIDATION`. **Décision v1s-15-5 (2026-07-23) — Option (b) conservé comme outil de secours manuel** : commentaires de tête de classe ajoutés sur `ValidateSaleService.java` et `pending_sales_page.dart` expliquant l'état "actuellement inatteignable, conservé comme outil de secours". Les services/controllers backend et les pages Flutter sont conservés tels quels, prêts à être réactivés si un futur flow (import CSV, API d'administration) réintroduit `PENDING_VALIDATION`. Fichiers Flutter : `pending_sales_page.dart`, `pending_sale_detail_page.dart`, `pending_sales_count_provider` (pos_providers.dart), `sale_success_page.dart` (variante ambre). Backend : `ValidateSaleService.java`, `SaleValidationCascadeService.java`, `PendingSaleController.java`.
- **Test mixed cart `[DRAFT, ACTIVE]`** : ajouter un test `checkout_page_test.dart` avec panier mixte (1 draft + 1 active) → label "🔶 Vente brouillon". `cart.any()` trivialement correct mais renforce la régression UI.
- **Failure-path draft flow** : scénarios où `applyInitialStockEntries` ou `promoteToActive` throw — pas de test de rollback/consistance. Hors scope story actuelle (happy path uniquement).
- **Magic strings `'ACTIVE'`/`'DRAFT'`/`'COMPLETED'`** : refactor modèle → enum (`ProductStatus`, `SaleStatus`). Pré-existant dans `cart_item.dart:14,32` et `record_sale_notifier.dart:108`. Modèle-wide, hors scope.
- **`addTearDown(container.dispose)` manquant** `record_sale_notifier_test.dart:182,235` : les tests AC3/AC4 créent un `ProviderContainer` sans le disposer. Pattern inconsistent dans le fichier (3 tests existants ne le font pas non plus).
- **Test empty cart rendering** : edge case où le panier est vide sur `CheckoutPage` — vérifier label "Valider la vente" + bouton désactivé. Couverture bonus.
- **Stale `draftInitialStocksProvider`** `record_sale_notifier.dart:66-67` : si un produit est retiré du panier entre `CartBottomSheet` et `CheckoutPage`, la map conserve l'entrée périmée et `applyInitialStockEntries` l'applique. Comportement pré-existant.
- **Sémantique accessibilité emoji 🔶** `checkout_page.dart:311` : séparer l'icône du label via `Semantics`/`ExcludeSemantics` pour screen readers. Cosmétique.

## Deferred from: code review of 10-1-flyway-baseline-schema-public (2026-06-19)

- **[10-1] Override `docker-java` 3.4.1 vs Testcontainers 1.20.4** (`pom.xml:17-29`) : seuls `docker-java-api` et `docker-java-transport-zerodep` sont épinglés ; si `docker-java-core` reste sur la version de Testcontainers, risque de `NoSuchMethodError`/`NoClassDefFoundError`. Vérifier `mvn dependency:tree`. Lié à la dette Testcontainers déjà déférée.

## Deferred from: code review of 10-6-archunit-modulith-verify-baseline (2026-06-19)

> Tous pour la **phase d'enforcement** (Epic 12.2e / Story 12.6) — la baseline warning-mode est correcte telle quelle.

## Deferred from: code review of v1s-12-5-tenantid-validate-jwtfilter (2026-07-21)

- **[v1s-12-5] D3 — OWNER + `tenantId` invalide → 500 au lieu de 401** : `JwtAuthFilter.java:130,190` — les tokens OWNER avec `tenantId` invalide peuplent `TenantContext` puis échouent sur `public` schema → 500. Intentionnel (Option B validation globale différée à la refonte).
- **[v1s-12-5] D4 — `jdbcTemplate=null` dans `setUp()`** : `JwtAuthFilterTest.java:50` — le filtre par défaut a `jdbcTemplate=null`. Risque NPE si un futur test EMPLOYEE utilise `filter` au lieu de `filterWithJdbc()`. Helper documenté.
- **[v1s-12-5] D5 — Message d'exception avec input attacker-controlled** : `TenantSchema.java:65` — concaténation du claim JWT brut dans le message. Non exploitable actuellement (catch + discard). Risque si loggé ailleurs sans assainissement.
- **[v1s-12-5] D6 — Pas de test EMPLOYEE INACTIVE avec `validate()`** : `JwtAuthFilterTest.java` — seul ACTIVE est couvert par `shouldAcceptValidTenantId`. Chemin INACTIVE pré-existant inchangé.
- **[v1s-12-5] D7 — 4 fichiers dupliquants non migrés vers `TenantSchema`** : `SchemaAwareMultiTenantConnectionProvider`, `TenantSchemaSyncService`, `TenantSchemaProvisioner` — explicitement hors-scope V1-stab. La refonte les fera converger.
- **[v1s-12-5] D8 — Pas de test `tenantId=null` EMPLOYEE** : `JwtAuthFilterTest.java` — redondant avec AC3 (même code path `IllegalArgumentException` → 401).
- **[v1s-12-5] D9 — Pas de test `tenantId=""` EMPLOYEE** : `JwtAuthFilterTest.java` — redondant avec AC3.
- **[v1s-12-5] D10 — `IncorrectResultSizeDataAccessException` (>1 row) → 500** : `JwtAuthFilter.java:155,183` — non catché. Pré-existant, >1 employé pour même `user_id` = bug d'intégrité données.
- **[v1s-12-5] D11 — `String.matches()` au lieu de `Pattern.compile()`** : `TenantSchemaSyncService.java:116` — pré-existant, impact négligeable (1× par tenant par JVM, caché).

- **[10-6] Convertir la baseline en ratchet à l'enforcement** : `verify()` + règles ArchUnit n'assertent rien (warning mode voulu). À l'activation, asserter le set de violations connues → échec sur toute NOUVELLE violation seulement.
- **[10-6] Corriger `allowedDependencies` (cible vs réel)** : `identity`/`messaging` nus mais importent plusieurs modules ; `catalog` sans `messaging` ; `store` sans `subscription`. Aligner avant enforcement.
- **[10-6] Wrapper `detectModulesAndGenerateDocumentation()` en try/catch** : sinon un modèle invalide fait échouer le build malgré l'intention warning-mode.
- **[10-6] Affiner règles ArchUnit** : pattern `..adapter.out.persistence..` rate `shared.infrastructure.persistence.entity` ; ajouter `withImportOption(DO_NOT_INCLUDE_TESTS)`.
- **[10-6] CI `upload-artifact@v4` : ajouter `if-no-files-found: warn`** (évite l'échec du job si les fichiers absents).
- **[10-6] Scope deps** : `spring-modulith-api` → envisager `provided` ; laisser le BOM Modulith gérer la version d'`archunit` (éviter skew vs 1.3.0 épinglé).

## Deferred from: code review of 10-5-environnement-staging (2026-06-19)

- **[10-5] 🔴 TLS staging — implémenter le routage via Traefik prod** (DÉCISION Toor) : réseau Docker externe partagé `keevo_edge` (traefik prod + keevo_staging_backend) ; router `staging.api.keevo.cm` + service `http://keevo_staging_backend:4500` dans le `traefik/dynamic.yml` prod ; retirer le Traefik staging/ACME/`:8443` ; DNS staging → VPS. Touche le pipeline prod → appliquer + tester au 1er bring-up staging. **Prérequis pour que staging serve du HTTPS valide.**
- **[10-5] Staging réutilise les secrets/clés JWT/Firebase prod** : blast radius partagé — une compromission staging avec la clé JWT prod réutilisée permet de forger des tokens prod. Générer un keypair JWT + service-account Firebase + credentials DB dédiés staging.
- **[10-5] `workflow_dispatch` sans garde de branche** (`deploy-staging.yml`) : un dispatch manuel peut builder/déployer staging depuis n'importe quel ref. Ajouter un check `github.ref`.
- **[10-5] `firebase-credentials.json` bind-mount inconditionnel + pas de garde `.env` 1er deploy** : fichier absent → Docker crée un répertoire au point de montage ; `.env` absent → `compose up` avec vars vides. Rendre le mount conditionnel + `[ -f .env ] || exit 1`.
- **[10-5] `cancel-in-progress: true`** (`deploy-staging.yml`) : peut interrompre un `docker compose up` SSH en cours → stack staging à moitié déployée. Désactiver l'annulation sur les étapes mutantes.
- **[10-5] prod `deploy-backend.yml` : clé privée JWT en `chmod 644`** ✅ **RÉSOLU** — story `v1s-12-1` (2026-07-21) : split `chmod 600` + `chown 1001:1001` (UID keevo pinné Dockerfile) + assertion pre-flight + grep lint AC4. Plus world-readable check Java (ProdSecretsValidator) en défense-en-profondeur.

## Deferred from: code review of 10-4-backups-postgresql-restauration (2026-06-19)

- **[10-4] systemd unit `/home/ubuntu/...` hardcodé** (`keevo-backup.service`) : `EnvironmentFile`/`ExecStart` figés sur `/home/ubuntu` alors que le deploy installe sous `${HOME}/keevo`. Casse si `DEPLOY_SSH_USER != ubuntu`. Templatiser via sed au deploy.
- **[10-4] `.env.backup` non créé/validé par le deploy** : le timer est activé+démarré mais le fichier est manuel ; 1er run échoue silencieusement. Ajouter `EnvironmentFile=-` (tolérant) + un check deploy-time qui WARN si absent.
- **[10-4] Aucun alerting sur échec backup** : journald only. Ajouter `OnFailure=keevo-backup-alert@.service` (email/webhook).
- **[10-4] S3 sans IAM least-privilege / bucket versioning / Object-Lock** : un attaquant sur la box peut supprimer les backups (ransomware). Compte IAM write-only + versioning + MFA-delete.
- **[10-4] Restore `--no-owner --no-acl` perd les grants/ownership** : le rollback prod (§7) ne ré-applique pas les privilèges → RLS/permissions potentiellement cassées post-rollback. Documenter l'étape de re-grant.
- **[10-4] `script_stop:false` + `systemctl status || true`** (`deploy-backend.yml`) : un install cassé (sudo/systemd) rapporte « succès ». Gater proprement l'échec.
- **[10-4] DEPLOYMENT_CHECKLIST §7 écrit dans `docs/` racine** au lieu de `keevo/backend/docs/` (divergence vs spec). Confirmer l'emplacement voulu.

## Deferred from: code review of 10-3-externalisation-secrets-fail-fast (2026-06-19)

- **[10-3] POSTGRES_PASSWORD non validé par le fail-fast** : `application.yml:17` → `${POSTGRES_PASSWORD:keevo_local_pwd}`. En prod, si l'env est absent, le boot hérite silencieusement du mot de passe DB dev. Hors périmètre AC 10.3 mais vrai risque prod — étendre `ProdSecretsValidator` au mot de passe DB (et tokens Twilio/Wassender quand provider actif).
- **[10-3] AC7/AC8 — fail-fast non testé au niveau câblage Spring** : les tests sont unitaires (appel direct `validate()`). Le `@Profile({"prod","staging"})` + `@PostConstruct` (échec réel du boot) n'est pas couvert. Ajouter un `@SpringBootTest @ActiveProfiles("prod")` (sans DB) prouvant l'`ApplicationContextException`.
- **[10-3] Pas de contrôle de robustesse du mot de passe admin** : seul `Admin@1234!` (exact, case-sensitive) est rejeté. Le placeholder `change_me_strong_password` de `.env.example`, `admin`, `123456` passent. Ajouter une politique de force (longueur/entropie) ou au moins normaliser (trim/case).
- **[10-3] DEPLOYMENT_CHECKLIST — rotation ADMIN_PASSWORD via env = no-op** : pour un utilisateur admin déjà créé, changer `ADMIN_PASSWORD` n'a aucun effet (pas de re-hash). La doc doit renvoyer vers l'API change-password.

## Deferred from: code review of 10-2-migrations-versionnees-fin-ddl-auto (2026-06-19)

- **[10-2] Commit hygiene — `admin.password` story 10-3 change dans working tree 10-2** : le défaut `Admin@1234!` a été supprimé dans `application.yml` avec un commentaire `Story 10.3`, mais ce changement se retrouve dans le diff de la story 10-2. Non bloquant fonctionnellement ; à ne pas reproduire dans les prochaines stories.
- **[10-2] `v2IsIdempotent` assert `migrationsExecuted==0` trop large** : dès que V3 sera créé, le test échouera de façon trompeuse si V3 est présent mais non encore appliqué au moment du test. Adresser lors de la création de V3 en restreignant l'assertion à "pas de nouvelle migration V2".
- **[10-2] `@BeforeAll` DROP DATABASE sans `pg_terminate_backend` préalable** (pre-existing, Story 10.1) : si un run précédent s'est crashé, le DROP échoue silencieusement et la suite démarre sur une DB stale avec des migrations déjà appliquées. Fix : ajouter `SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='keevo_flyway_test'` avant le DROP.
- **[10-2] `@ActiveProfiles("flyway-test")` sans fichier `application-flyway-test.yml`** (pre-existing, Story 10.1) : l'annotation est trompeuse. Si un futur développeur crée ce fichier de profil, il pourrait écraser le `@DynamicPropertySource` et pointer le test vers une mauvaise datasource. Envisager de supprimer l'annotation ou de créer un fichier minimal.
- **[10-2] DDL_USERS dans TenantSchemaProvisioner incomplet** (pre-existing) : `failed_attempts`, `locked_until`, et potentiellement d'autres colonnes sont absentes du DDL statique — elles sont ajoutées dynamiquement par `TenantSchemaSyncService` au premier login. Fenêtre de vulnérabilité entre la création du tenant et son premier accès authentifié.

## Deferred from: code review of v1s-12-1-chmod-jwt-private-key (2026-07-21)

- **Tests `PosixFilePermission` breakent sur runner non-POSIX (Windows)** : `shouldRejectWorldReadablePrivateKey` / `shouldRejectGroupReadablePrivateKey` utilisent `Files.setPosixFilePermissions` et `assertThatThrownBy`. Sur un FS non-POSIX, le check Java skip via `UnsupportedOperationException` → pas d'exception → `assertThatThrownBy` FAIL. CI tourne sur `ubuntu-latest` (POSIX) donc non-bloquant maintenant, mais un futur runner Windows ou un dev sous Windows casserait le build. À adresser si la matrice CI s'élargit.

## Deferred from: code review of v1s-12-3-superadmin-path-enforcement (2026-07-21)

- **[v1s-12-3] Garde par-méthode `requireSuperAdmin()` devient code mort pour le rejet** : la filter-chain `hasRole("SUPER_ADMIN")` bloque tout non-SUPER_ADMIN avant le contrôleur. `AdminTenantControllerTest` OWNER→403 passe désormais via la filter-chain (pas via `requireSuperAdmin()` jetant `DomainException`). Une régression supprimant `requireSuperAdmin()` ne serait plus attrapée par ce test. By-design layered defense (couche externe gagne) ; AC2 = préservation vérifiée par inspection. À réévaluer lors de la refonte (Epic 17) — décider si le garde par-méthode est gardé comme seconde barrière ou retiré comme code mort.
- **[v1s-12-3] Trous de couverture `SecurityConfigAdminPathEnforcementTest`** : pas de cas rôle EMPLOYEE, pas d'anonymous→401, pas de token authentifié-sans-rôle, seul GET exercé. La règle est role/method-agnostic donc le comportement est impliqué, mais les branches ne sont pas assertées. Non requis par AC1-AC5 ; durcissement optionnel (ajouter 3-4 tests).
- **[v1s-12-3] `SecurityContextHolder` non nettoyé entre tests** : `setupFilterAs` ne réplique pas le `SecurityContextHolder.clearContext()` de `JwtAuthFilter:197`. Le `doAnswer` réécrit le contexte à chaque `perform` donc pas de fuite aujourd'hui, mais un futur test omettant `setupFilterAs` hériterait de l'auth du test précédent (faux-green). Convention codebase-wide (aucun test admin n'a `@AfterEach clearContext`). Hygiene optionnelle : ajouter `@AfterEach void tearDown() { SecurityContextHolder.clearContext(); }`.
- **[v1s-12-3] Magic string `"SUPER_ADMIN"` dupliquée** : `SecurityConfig.java:96` (`hasRole`), `JwtAuthFilter.java:181` (`"ROLE_" + role`), `AdminAccountInitializer`, corps `requireSuperAdmin()`. L'enum `Role` existe mais n'est pas référencé au niveau filtre — un rename de l'enum ne remonterait pas en erreur compile à la règle filter-chain. Pre-existing, codebase-wide. Centraliser via constante/enum.
- **[v1s-12-3] `accessDeniedHandler` rend HTML, pas JSON** : `SecurityConfig.java:83-84` `res.sendError(403)` déclenche la page d'erreur servlet (HTML pour navigateurs) divergeant du javadoc classe « 403 JSON response for authenticated but unauthorized requests ». Pre-existing ; la règle admin augmente le trafic vers ce handler. Aligner sur JSON (e.g. `res.setStatus(403); res.setContentType("application/json"); res.writer.write(...)`).
- **[v1s-12-3] `@MockBean` déprécié** : Spring Boot 3.5.0, `@MockBean` déprécié depuis 3.4+ en faveur de `@MockitoBean`. Le nouveau test propage la déprécation (cohérent avec les tests admin existants). Migration codebase-wide `@MockBean`→`@MockitoBean` à planifier.

## Deferred from: code review of v1s-12-2-session-revocation-5min (2026-07-21)

- **`iat` futur / pas d'upper-bound de clock-skew** [`JwtAuthFilter.java:141`] — Pas de `setAllowedClockSkewSeconds` au parse ; un `iat` futur biaisé traite un token minté avant révocation comme post-révocation (accepté). RS256 empêche le forgage mais un skew légitime multi-région crée une petite fenêtre. Partiellement mitigé par le patch granularité-seconde. Refonte : adopter une politique de clock-skew (jjwt `setAllowedClockSkewSeconds` + reject `iat` loin dans le futur).
- **Cache `ConcurrentHashMap` non borné** [`CachedTokenRevocationAdapter.java:40`] — Pas d'éviction ni de size cap ; les entrées `(userId,tenant)` persistent pour la durée de vie JVM (users dormants/supprimés, tenants churned). La claim spec « bounded by active pairs » est fausse. Fuite mémoire lente. Refonte : Caffeine `expireAfterWrite(5min).maximumSize(...)` ou sweeper périodique.
- **Race de re-population de cache stale (read-then-put non atomique)** [`CachedTokenRevocationAdapter.java:54-55`] — Thread A (cache miss) lit l'ancien tva via delegate, puis avant `cache.put` le Thread B exécute `revokeAllSessions` (DB update + `cache.remove`) ; le Thread A réinsère alors l'ancien tva avec un `fetchedAt` frais → cache stale servi jusqu'à TTL sur la MÊME instance qui a révoqué. Fenêtre minuscule, requiert une requête concurrente du principal révoqué au moment exact de la révocation. Refonte : `computeIfAbsent`/`compute` atomique ou tombstone.
- **Cache stampede / thundering herd sur miss** [`CachedTokenRevocationAdapter.java:49-55`] — Check-then-put non atomique ; N requêtes concurrentes sur un cache miss chaud déclenchent chacune un `SELECT m.tokens_valid_after`. Idempotent (pas de défaut de correction), mais contrecarre l'objectif de protection DB juste sous charge burst. Refonte : coalescence des loads (`ConcurrentHashMap<String,CompletableFuture<Instant>>`).
- **`shouldCallDelegateOnCacheExpiry` n'exerce pas l'expiry TTL** [`CachedTokenRevocationAdapterTest.java:42-60`] — Le test admet utiliser une clé différente (« TTL is real-time, not mockable ») au lieu d'avancer le TTL ; la branche `cached.fetchedAt().plusSeconds(TTL_SECONDS).isAfter(now)` n'est jamais exécutée. Fausse couverture AC7 (cache expiré >TTL → delegate rappelé). Un bug d'arithmétique TTL passerait. La logique production est triviale et correcte à l'inspection. Refonte : injecter un `java.time.Clock` dans `CachedTokenRevocationAdapter` pour rendre le TTL testable (avancer le temps dans le test et ré-querier la MÊME clé).
- **`JdbcTokenRevocationAdapterTest` utilise `anyString()` et contourne le `RowMapper`** [`JdbcTokenRevocationAdapterTest.java:25-44`] — `when(jdbcTemplate.queryForObject(anyString(), any(RowMapper.class), ...)).thenReturn(now)` retourne un `Instant` pré-construit, donc le `RowMapper` réel (`rs.getTimestamp("tokens_valid_after").toInstant()`) ne tourne jamais et la string SQL n'est pas assertée. Un rename de colonne ou une mauvaise table casserait le runtime tout en gardant les tests GREEN. Refonte : `ArgumentCaptor<RowMapper>` invoqué sur un `ResultSet` mocké (`when(rs.getTimestamp("tokens_valid_after")).thenReturn(...)`) + `eq(SELECT_TVA)`.
- **Champ `log` inutilisé (dead code, zéro observabilité)** [`CachedTokenRevocationAdapter.java:32`] — `private static final Logger log` déclaré, jamais référencé. Pour un contrôle de sécurité avec fenêtre de propagation 5min, aucun événement d'invalidation/miss/race-stale n'est observable. Refonte : `log.debug` sur cache miss + invalidate (au minimum), DEBUG sur stale-race.
- **Tests manquants : path `tva==null` et frontière `iat==tva`** [`JwtAuthFilterTest.java:345-408`] — Seuls `iat<tva` (reject) et `iat>tva` (allow) couverts. Manquent : (a) `getTokensValidAfter` retourne null → check skippé, requête procède (path dominant pour tout user non révoqué) ; (b) `iat == tva` (AC2 stipule `>=` → allow ; le code utilise `isBefore` strict `<` donc l'égalité est acceptée — correct mais la charnière de contrat n'est pas gardée). Un futur refactor vers `!iat.isAfter(tva)` (`<=` → reject) violerait AC2 en passant les tests existants. Refonte : ajouter `shouldAllowWhenTokensValidAfterNull` + `shouldAcceptTokenWhenIatEqualsCutoff`.
- **Latency OWNER : check révocation DB per-request sur cache miss** [`JwtAuthFilter.java:138`] — Le rôle OWNER avait 0 hit DB dans le filtre ; la story 12.2 ajoute un `SELECT m.tokens_valid_after` (toutes les 5min/user sur cache miss). By-design — le cache absorbe l'essentiel. Refonte : envisager un JOIN unique avec le check EMPLOYEE `queryForMap` pour mutualiser le round-trip.

## Deferred from: code review of v1s-12-7-flutter-hardening-storage-logs-https (2026-07-21)

- **[v1s-12-7] Race condition SESSION_REVOKED vs TOKEN_EXPIRED refresh concurrent** [`auth_interceptor.dart:91-143`] : si Request A est en refresh (`_isRefreshing=true`) et Request B reçoit SESSION_REVOKED, le garde `!_isRefreshing` (ligne 91) empêche B d'entrer dans le handler SESSION_REVOKED → B tombe en `handler.next(err)` sans nettoyage. Auto-cicatrisant (prochaine requête recevra aussi 401 avec `_isRefreshing=false`). Fix : flag `_sessionDead` cross-path, à traiter dans story de suivi sécurité.
- **[v1s-12-7] SnackBar flood sur SESSION_REVOKED concurrent** [`auth_provider.dart:128-136`] : N requêtes simultanées recevant 401 SESSION_REVOKED déclenchent N SnackBars « Votre accès a été suspendu » en file d'attente. Fix : debounce `rootScaffoldMessengerKey` ou flag `_suspendedShown` dans l'interceptor — hors scope V1-stab.
- **[v1s-12-7] Lecture secure storage à chaque navigation (AC5)** [`app_router.dart:353-357`] : chaque transition de route déclenche `storage.read(key: 'jwt_token')` + décodage AES via `EncryptedSharedPreferences` (10-50ms). Spéc-mandaté par AC5. Optimisation : cache Riverpod synchronisé à login/logout/refresh — changement architectural hors scope.
- **[v1s-12-7] Duplication `badCertificateCallback` verbatim** [`auth_provider.dart:78-85,103-110`] : même bloc 9 lignes copié sur `dioProvider` et `_refreshDioProvider`. Fix : extraire `_createDebugHttpClient()` helper — cosmétique DRY.

## Deferred from: code review of v1s-12-6-rbac-owner-only-scope-store (2026-07-21)

- **[v1s-12-6][CRITIQUE] `/api/v1/sync/push` contourne intégralement le durcissement RBAC de cette story** [`sync/sync/application/handler/CancelSaleSyncHandler.java`, `ValidateSaleSyncHandler.java`, `StockAdjustSyncHandler.java`, `TransferSyncHandler.java`] : ces 4 handlers appellent directement les use cases (`cancelPendingSaleUseCase`, `validateSaleUseCase`, `adjustStockUseCase`/`recordStockEntryUseCase`, `transferStockUseCase`) sans passer par les controllers REST durcis par cette story, et sans aucun contrôle de rôle ni de scope storeId (`CancelSaleSyncHandler`/`ValidateSaleSyncHandler` passent `assignedStoreId=null` codé en dur, quel que soit le rôle réel de l'appelant). Un EMPLOYEE peut ainsi annuler/valider des ventes de n'importe quelle boutique et ajuster du stock/initier des transferts hors de sa boutique, simplement en passant par la synchronisation offline plutôt que par l'API REST directe — annulant une bonne partie de la valeur sécurité de Story 12.6 (AC2/AC5/AC7, FR36, B-HIGH-9/10). Fichiers pré-existants, non touchés par cette story. Recommandation : story de suivi immédiate, priorité release-blocker.
- **[v1s-12-6] `StockTransferController.complete()` sans scope storeId destination** [`StockTransferController.java:99-107`, `CompleteTransferService.java`] : aucun contrôle ne vérifie que l'EMPLOYEE qui réceptionne un transfert est bien affecté à la boutique destination — n'importe quel EMPLOYEE peut compléter n'importe quel transfert `IN_TRANSIT`. Contraire à FR36 et à la justification même de l'AC7 (« l'employé à la boutique destination doit pouvoir réceptionner »). Pré-existant (Story 3.3), explicitement laissé hors scope par cette story (Task 6.3). À couvrir dans une story de suivi FR36.
- **[v1s-12-6] Duplication du helper `isOwnerRole()`/`requireOwner()`** across `PendingSaleController`, `DayClosureController`, `StoreController`, `StockTransferController` : logique de contrôle de rôle identique copiée-collée 4 fois plutôt que factorisée. Cohérent avec le principe « scope chirurgical » déjà validé en code review Story 12.5 — pas bloquant, à regrouper si un 5ᵉ controller OWNER-only apparaît.

## Deferred from: code review of v1s-13-1-optimistic-lock-stock-transfer (2026-07-21)

- **[v1s-13-1] Pas de test d'intégration `@Modifying` + `@Version` avec EntityManager réel** [`StockTransferSpringRepository.java:31-36`] — Le JPQL `@Modifying` bypass le persistence context Hibernate et le `@Version` automatique. La seule façon de valider que `clearAutomatically=true` fonctionne correctement (et que `findById` après transitionStatus lit bien la DB) est un test `@DataJpaTest` ou `@SpringBootTest` avec un vrai EntityManager. Nécessite Testcontainers/PostgreSQL — hors scope AC5 (tests unitaires). Recommandation : ajouter dans Epic 15 (dette tests) ou lors de la prochaine story stock.
- **[v1s-13-1] Pas de test concurrent multi-thread sur `CompleteTransferService.execute()`** [`CompleteTransferService.java:50-104`] — L'AC5 exige `shouldNotDoubleCreditOnConcurrentTransferComplete()` mais les tests existants sont unitaires mockés. Ils ne vérifient pas le comportement sous concurrence réelle (deux threads appelant `execute()` simultanément avec `CountDownLatch`). Nécessite infrastructure de test concurrent — hors scope AC5. Recommandation : idem ci-dessus.
- **[v1s-13-1] V5 migration `public` + `TenantSchemaSyncService` boot-time dependency** [`V5__stock_optimistic_lock.sql`, `TenantSchemaSyncService.java:230-232`] — Pattern pre-existant (toutes les migrations ALTER TABLE per-tenant ont le même risque) : V5 s'applique au schema `public` via Flyway, puis `TenantSchemaSyncService.ensureRequiredTenantTables()` propage aux tenants existants au boot. Si SyncService échoue silencieusement (exception catchée), le schema du tenant n'a pas la colonne `version` → tout `save()` sur `StockTransferJpaEntity`/`StockLevelJpaEntity` échoue avec "column version does not exist". Non nouveau à cette story — même risque pour `updated_at` (Story 5.2), `minimum_threshold`, etc. Recommandation : monitorer les erreurs SyncService au boot (log ERROR visible).

## Deferred from: code review of v1s-13-2-sync-gate-7d-unblock (2026-07-21)

- **[v1s-13-2] TOCTOU race condition sur pull endpoint** [`SyncController.java:146-157`] — Le ownership check (findByDeviceId) et l'upsertOnPull ne sont pas atomiques. Scénario théorique : T1 (User A) et T2 (User B) font pull avec le même deviceId simultanément. Les deux passent le check (device inexistant), puis T1 upsert ownership à A, T2 upsert ownership à B (écrase A). Risque pratique faible (nécessite coordination attaquant+victim au même device, deviceId partagé). Recommandation future : upsert atomique avec contrainte UNIQUE sur device_id + ON CONFLICT, ou optimistic lock.
- **[v1s-13-2] Logging de sécurité manquant sur échec ownership check** — Quand le ownership check échoue (DEVICE_ID_MISMATCH 403), aucun log de sécurité n'est émis. Pour un contrôle marqué B-CRIT-3, cela complique l'investigation d'incidents. Pré-existant : le module sync n'utilise pas AuditPort. Recommandation : story de normalisation du logging security pour uniformiser le pattern.
- **[v1s-13-2] Risque information disclosure — fuite existence device** — Un attaquant peut distinguer « device existe mais mauvais owner » (403 DEVICE_ID_MISMATCH) de « device n'existe pas » (push réussi ou gate check). Permet d'énumérer les deviceIds valides. Threat model spéculatif (l'endpoint `/api/v1/sync/devices` expose déjà les deviceIds mais requiert auth). Recommandation future : retourner erreur générique ou rate-limiter les échecs.

## Deferred from: code review of v1s-13-3-dayclosure-weekly-eod-fix (2026-07-21)

- **[v1s-13-3] CloseDayService clock injection** [`CloseDayService.java:56-58`] — `LocalDate.now(WAT_ZONE)` direct, pas de `Clock` injecté. Incohérent avec `DayClosureAutoScheduler` qui injecte un `Clock` pour testabilité. Pre-existing (pas introduit par cette story). Recommandation : injecter `Clock` dans `CloseDayService` (constructor + package-private test ctor, mirror scheduler).
- **[v1s-13-3] TOCTOU race condition scheduler** [`DayClosureAutoScheduler.java:122-134`] — Pattern check-then-act : `existsByStoreIdAndDate(store.id(), todayWAT)` puis `closeDay()` non atomique. Si deux instances scheduler tournent simultanément (horizontal scaling), double closeDay possible. Pre-existing (ancien code avait le même pattern). Recommandation : distributed lock ou contrainte UNIQUE sur (store_id, report_date) dans day_closures.
- **[v1s-13-3] Hourly cron overhead** [`DayClosureAutoScheduler.java:86`] — Cron horaire `0 0 * * * *` = N tenants × M stores × 24 checks/jour. Décision spec AC4 Option A (mirror WeeklyReportScheduler). Acceptable échelle actuelle (<100 tenants). À surveiller si scale-up (monitorer latence DB sur `existsByStoreIdAndDate`).
- **[v1s-13-3] parseTime format validation** [`DayClosureAutoScheduler.java:141-149`] — Formats invalides (`"25:00:00"`, `"20:00"` sans secondes) → fallback silencieux vers 20:00 via `parseTime()`. Pas de validation à l'écriture. Recommandation : valider dans `TenantPreferencesRepository.save()` ou dans le controller PUT /api/v1/tenant/preferences (reject invalid time format avec 422).
- **[v1s-13-3] Day closure window — ventes après trigger time** [`CloseDayService.java:67-68`] — Closure à 20h (eodReportTime) capture fenêtre 00:00-23:59, mais ventes 20:01-23:59 pas encore en DB à 20h. Question design : clôturer à 20h (UX13 — fermeture boutique) ou à 23:59 (complétude données) ? Décision produit requise. Recommandation : documenter le comportement dans le PRD/AC (Story 4.4/7.6) — les ventes après 20h sont perdues pour la journée et rattachées au jour suivant.
- **[v1s-13-3] Idempotency across scheduler restarts** [`DayClosureAutoScheduler.java:91-103`] — Si scheduler crash à 20:30 après avoir traité 50% des tenants, restart à 21:00 re-traite tous. `existsByStoreIdAndDate` skip les tenants déjà clôturés, mais tenants mid-closure (exception pendant `closeDay`) pourraient recevoir double closeDay. Pre-existing. Recommandation : distributed lock ou idempotency key (correlation ID) pour garantir single-execution.

## Deferred from: code review of v1s-13-4-sync-queue-drift-atomic (2026-07-22)

- **[v1s-13-4] `SyncService` Strategy swap non garanti compile-time/test** [`keevo/app/lib/core/sync/sync_service.dart`] — Rien n'assure qu'une future implémentation de `SyncService` (ex. `PowerSyncService`, évoquée en commentaire dans l'interface) partage la même connexion Drift `AppDatabase` que `recordSale()`. Si ce n'est pas le cas, la garantie d'atomicité F-HIGH-4 régresse silencieusement — aucun signal compilateur ni test ne le détecterait (les tests mockés avec `FakeAppDatabase` ne peuvent pas le prouver). Déjà documenté consciemment dans les Dev Notes de la story ("Confirmation architecturale"). Pas actionnable maintenant : aucune seconde implémentation de `SyncService` n'existe à ce jour. Recommandation : si/quand une seconde implémentation apparaît, ajouter un test d'intégration prouvant le partage de connexion, ou documenter explicitement le contrat dans l'interface `SyncService`.
- **[v1s-13-4] Double-application possible du stock initial sur retry après échec `recordSale()`** [`keevo/app/lib/features/pos/presentation/provider/record_sale_notifier.dart:70-83,124`] — `applyInitialStockEntries()`/`promoteToActive()` (produits brouillon) s'exécutent **avant** et **hors** de la transaction de `recordSale()`. Si `recordSale()` échoue ensuite (ex. crash mid-enqueue, désormais rollback garanti par cette story) et que l'utilisateur retente l'action, le panier n'est pas vidé sur erreur (`catch (e) { state = RecordSaleError(...) }`, ligne 124, pas de `clearCart()`) — un second essai ré-exécuterait `applyInitialStockEntries()` avec les mêmes quantités, risquant un double-crédit de stock initial pour les produits brouillon. Mécanisme antérieur à cette story (elle ne fait que rendre le rollback de la vente elle-même plus fiable) ; déjà signalé hors-scope dans la section "Out of scope" de la story elle-même (pitfall #4 "applyInitialStockEntries()"). Recommandation : idempotency guard sur `applyInitialStockEntries()` (ex. clé produit déjà promu → skip) ou déplacer l'étape dans la même transaction que `recordSale()`.
- **[v1s-13-4] `kDebugMode` non résolu casse la compilation d'~68 fichiers de test** [`keevo/app/lib/core/router/app_router.dart:139`] — **RÉSOLU par v1s-13-5** : la story v1s-13-5 avait besoin d'exécuter `sale_detail_page_test.dart` (Task 7.4, importe transitivement `app_router.dart`) et ne pouvait donc pas laisser ce blocker non résolu sans renoncer à toute vérification TDD de sa propre Task 7. Cause exacte identifiée : import manquant de `package:flutter/foundation.dart` (un seul import ajouté en tête de fichier, aucune autre logique touchée). Après correctif, `flutter test` complet passe de 522 passed/68 failed (tous par échec de compilation, aucune assertion réellement exécutée) à 803 passed/12 failed — voir entrée suivante pour le détail des 12 échecs désormais visibles.

## Deferred from: dev-story of v1s-13-5-fr90-sale-cancellation (2026-07-22)

- **[v1s-13-5] 12 échecs de test pré-existants démasqués par le fix `kDebugMode`** — Après correction du blocker `kDebugMode` (entrée précédente), 12 tests jusqu'ici invisibles (leur fichier ne compilait même pas) échouent réellement pour des raisons **sans aucun rapport avec cette story** (aucun dans les modules `commerce/sale`, `pos`, `catalog/stock` touchés par v1s-13-5) : `core/sync/offline_gate_banner_test.dart` (couleur ambre attendue `#FFF3BF` ne correspond pas à la couleur réelle du thème — bug de valeur codée en dur), `features/inventory/data/repository/transfer_repository_backend_first_test.dart` (3 tests, `MockSyncService.queueOperation` non stubbé → `type 'Null' is not a subtype of type 'Future<void>'`), `features/contact/data/repository/{client,supplier}_repository_backend_first_test.dart` (4 tests, même cause), `features/catalog/data/repository/product_repository_backend_first_test.dart` (3 tests, même cause), `features/catalog/provider/category_provider_test.dart` (1 test). Confirmé par exécution isolée de chaque fichier — comportement inchangé par les modifications de cette story. Recommandation : story de hygiène dédiée pour (a) le pattern de stub manquant `MockSyncService.queueOperation` répété dans 4 fichiers de test `*_backend_first_test.dart` hors du module POS, (b) la valeur de couleur ambre du thème vs l'attente du test `offline_gate_banner_test.dart`.

## Deferred from: code review of v1s-13-5-fr90-sale-cancellation (2026-07-22)

- **[v1s-13-5] Aucun verrou optimiste sur `Sale` pour cancel/correct COMPLETED** [`CorrectSaleService.java:91-95`, `ValidateSaleService.java` (branche COMPLETED)] — un double-cancel concurrent peut restaurer le stock deux fois ; `CorrectSaleService` fige `SaleStatus.COMPLETED` au `save()`, donc une correction concurrente à une annulation peut silencieusement ré-écraser `CANCELLED` en `COMPLETED` (lost update, annule l'annulation). Même classe de bug que celle corrigée par v1s-13-1 (`@Version` StockTransfer/StockLevel) — `Sale` ne devient mutable en COMPLETED qu'à partir de cette story. **Deferred (décision review) : scope-cut / fenêtre d'exploitation étroite** — nécessiterait une nouvelle migration + changement d'entité, exige deux requêtes quasi-simultanées sur la même vente, action rare OWNER-only. Recommandation : story de suivi dédiée, mirror v1s-13-1, ajouter `@Version` à `SaleJpaEntity`/`Sale` + mapping `OptimisticLockException` → 409 sur les deux chemins cancel/correct.
- **[v1s-13-5] Aucune interaction avec la clôture de journée / les rapports** [`sale_detail_page.dart:10` (import mort `day_closure_providers.dart`), `CorrectSaleService.java`, `ValidateSaleService.java`] — rien n'empêche d'annuler/corriger une vente dont la journée est déjà clôturée (Story 13.3) ou déjà incluse dans un rapport hebdomadaire, ce qui invaliderait silencieusement des rapports financiers historiques. **Deferred (décision review) : décision produit requise** (bloquer/avertir/régénérer rapport ?), même catégorie que l'ambiguïté de fenêtre EOD déjà différée en Story 13.3. Recommandation : trancher le comportement voulu avec le PO puis implémenter la garde correspondante dans `ValidateSaleService`/`CorrectSaleService` (nouveau code erreur type `SALE_DAY_CLOSED` si blocage retenu).
- **[v1s-13-5] Test JPA merge-en-place jamais exécuté (Task 5 / Piège #6)** [`SaleRepositoryAdapterTest.java`] — `save_calledTwiceWithSameItemIds_updatesInPlace_noOrphanRemovalDuplication` a été écrit mais bloqué par la limitation préexistante H2/JSONB du bac à sable, jamais exécuté réellement. Le point de risque le plus élevé identifié par la story elle-même (duplication possible des `sale_items` par delete-then-reinsert lors d'une correction, `orphanRemoval=true`) reste formellement non vérifié par un test qui passe. Recommandation : rejouer ce test dès qu'un environnement Postgres réel est disponible (voir aussi Story 15.1 backlog Testcontainers).
- **[v1s-13-5] Corruption silencieuse du snapshot d'audit sur échec de sérialisation JSON** [`ValidateSaleService.java`, `CorrectSaleService.java:103-109`] — `serializeItems()` capture `JsonProcessingException` et renvoie `"[]"` sans log, motif répliqué depuis `RecordSaleService` (préexistant, pas introduit par cette story). Recommandation : story transversale pour ajouter un log d'alerte sur tous les sérialiseurs d'événements du module `commerce/sale` en cas d'échec, plutôt qu'un correctif isolé par story.
- **[v1s-13-5] Fenêtre TOCTOU négligeable dans la garde AC5 de `CancelSaleSyncHandler`** [`CancelSaleSyncHandler.java:54-62`] — `apply()` vérifie le statut via son propre `findById`, puis `cancelPendingSaleUseCase.cancelPendingSale()` re-charge et re-switch en interne ; fenêtre de course théorique entre les deux lectures séquentielles in-process, mais exploitabilité pratique négligeable (exige qu'une autre transaction commite un changement de statut dans l'intervalle microseconde). Recommandation : si un jour jugé prioritaire, faire porter le statut déjà chargé par `apply()` dans la commande plutôt que de laisser `cancelPendingSale()` re-lire.
- **[v1s-13-5] Trou RBAC sync adjacent sur l'annulation PENDING via `CancelSaleSyncHandler`** [`CancelSaleSyncHandler.java`] — aucun contrôle de rôle sur l'annulation d'une vente PENDING via sync (le même raisonnement que le correctif AC5 de cette story s'applique), mais explicitement hors scope par le texte même d'AC5. Déjà suivi comme defer CRITIQUE de v1s-12-6 (`sprint-status.yaml` ligne 37) — doublon volontaire noté ici pour visibilité, aucune nouvelle action requise au-delà du suivi 12.6 existant.
- **[v1s-13-5] Fuite de `TextEditingController` dans `sale_detail_page.dart`** [`sale_detail_page.dart:186-187,256-260`] — les contrôleurs créés dans `_showCancelDialog`/`_showCorrectSheet` (+ un par article dans `qtyControllers`) ne sont jamais disposés. Impact mineur (action rare, déclenchée par le propriétaire), correction propre nécessiterait de restructurer `_SaleDetailContent` en widget stateful ou d'utiliser un `StatefulBuilder`/`AutoDispose` dédié au dialogue. Recommandation : à corriger si ce module Flutter est retouché pour une autre raison, pas prioritaire seul.

## Deferred from: code review of v1s-13-7-offline-inventory-audit-events (2026-07-22)

- **[v1s-13-7] UNIQUE constraint violation sur variant mismatch** [`validate_inventory_provider.dart:91-131`, `stock_levels_table.dart:31-33`] — le filtre variant-aware peut retourner 0 rows quand le `variantId` du count ne matche pas celui de la ligne `stock_levels` existante, puis l'INSERT AC4 viole la contrainte UNIQUE `(product_id, store_id)` (sans `variant_id`). Spéc AC3 documente explicitement que c'est une limitation pré-existante du schéma ("variantId est toujours null en pratique aujourd'hui") et que c'est hors-scope. Candidat Story 14.1 (Variant bounded context) pour extension de l'index unique. Le code suit le même motif défensif établi dans `local_stock_transfer_datasource.dart:21-29`.
- **[v1s-13-7] Session avec zéro counts marquée VALIDATED silencieusement** [`validate_inventory_provider.dart:166-172`] — le `for` loop sur `counts` vide ne fait rien, mais le status update inconditionnel passe quand même → session marquée `VALIDATED` avec `completedAt` et `adjustmentsApplied=0`. Comportement pré-existant (inchangé par cette story).
- **[v1s-13-7] Re-validation d'une session VALIDATED écrase `completedAt` silencieusement** [`validate_inventory_provider.dart:166-172`] — pas de garde contre un double `validate()` sur une session déjà `VALIDATED`. L'AC2 `quantityDelta==0` skip empêche les doublons d'ajustement, mais `completedAt`/`updatedAt` sont écrasés à chaque appel. Comportement pré-existant (inchangé par cette story).
- **[v1s-13-7] AC4 INSERT hardcode `minimumThreshold: 0`** [`validate_inventory_provider.dart:129`] — quand aucune ligne `stock_levels` n'existe, la création utilise `minimumThreshold: 0` sans égard au seuil produit côté backend. By design (spec Task 3.4) : backend = source of truth, le seuil correct sera syncé à la prochaine pull. Documenté ici pour mémoire.

## Deferred from: code review of v1s-14-11-employee-full-edit (2026-07-23)

- **[v1s-14-11] bcrypt DoS — pas de `@Size(max)` sur `newPassword`** [`SetPasswordRequestDto.java:11`] — `@NotBlank` seulement, pas de plafond. Un attaquant peut envoyer un mot de passe de plusieurs Mo, saturant le CPU (bcrypt cost 12). Pré-existant : `ChangePasswordService` n'a pas de max non plus. Recommandation : story transversale pour ajouter `@Size(min=8, max=128)` sur tous les DTOs de mot de passe.
- **[v1s-14-11] Phone normalization — `+237...` vs `237...` stockés différemment** [`UpdateEmployeeService.java:83-96`] — la regex `^\+?[0-9]{8,15}$` accepte les deux formats, mais la vérification d'unicité est un exact string match. Deux users peuvent coexister avec le même numéro sous deux formats. Pré-existant : `RegistrationRequest` ne normalise pas non plus. Recommandation : story transversale pour normaliser (toujours stocker avec `+` E.164).
- **[v1s-14-11] TOCTOU race anti-lockout (pas de `SELECT FOR UPDATE`)** [`ChangeEmployeeRoleService.java:95-99`] — deuxOwners concurrents peuvent tous deux lire `size() == 2`, passer le guard, et se démettre simultanément → 0 OWNER actif. Aucun locking pessimiste dans le codebase. Recommandation : si jugé prioritaire, ajouter `SELECT ... FOR UPDATE` ou un advisory lock tenant-level.
- **[v1s-14-11] Flutter `UpdateEmployee` n'invalide pas `EmployeeList`** [`employee_provider.dart:143-164`] — après un update réussi, la liste affiche encore les anciennes données jusqu'au refresh manuel. Pattern consistant avec `DeactivateEmployee`, `ReassignStore` (aucun n'invalide). Recommandation : story Flutter d'harmonisation de l'invalidation cache.
- **[v1s-14-11] UI pages AC6 pending** [`edit_employee_page.dart`, `EmployeeCard`, router] — spec acknowledge "UI pages pending". edit_employee_page.dart, section sécurité, dialogs de confirmation, router wiring, build_runner — tous à livrer. Recommandation : prochaine story Flutter dédiée.

## Deferred from: _bmad-output/deferred-work.md (migré depuis legacy, 2026-07-23)

> **Audit v1s-15-5** : toutes les entrées ci-dessous ont été vérifiées contre le code actuel (2026-07-23) et confirmées toujours valides. Les entrées résolues (cancelSale RBAC, totalTransactions dupliqué, B3.1-B3.4 badge/bannière/onglet/montant, Ventes brouillon offline) ne sont PAS migrées — voir Dev Agent Record de v1s-15-5 pour le détail.

### Migré depuis: code review of story-10.1-flyway-baseline (2026-06-18)

- **D1 — Test utilise conteneur PG existant au lieu de Testcontainers (AC5)** : `FlywayBaselineIntegrationTest` se connecte à un conteneur PostgreSQL existant via `@DynamicPropertySource` (localhost:5444) au lieu d'utiliser `@Container PostgreSQLContainer<?>`. Cause : Docker API version mismatch (1.32 vs 1.40+). Le test valide AC3/AC4 mais n'est pas CI-portable. Fichier : `shared/infrastructure/persistence/FlywayBaselineIntegrationTest.java`.
- **D2 — CHECK constraints non idempotentes dans V1** : Les CHECK constraints (inventory_sessions scope/status, products status, stock_movements movement_type, stock_transfers status, stores type) sont définies inline dans `CREATE TABLE IF NOT EXISTS` au lieu de blocs `DO $$ IF NOT EXISTS` séparés. Sur base existante (tables créées par Hibernate), les CHECK inline sont silencieusement ignorées. Risque faible : Hibernate gère ses propres CHECK et Story 10.2 a migré vers `ddl-auto=validate`. Fichier : `V1__baseline_public.sql`.

### Migré depuis: code review of 9-1-gestion-des-tenants (2026-05-04)

- **D1 — `TenantsPage` — `"use client"` direct sur la route page**: La spec (task 3.8) demande un Server Component shell qui render un Client Component `<TenantsClientPage />`. L'implémentation met `"use client"` directement sur `app/tenants/page.tsx`. Pattern fonctionnel mais diverge de la spec. Impact : perd les bénéfices SSR potentiels pour le shell. Corriger lors d'un refactor Next.js layout.
- **D2 — N+1 `SELECT schema_name` dans `enrichWithCrossSchemaMetrics`**: Pour chaque ligne retournée par la liste, `safeLoadSchemaName(item.id())` émet une requête supplémentaire. Acceptable pour Phase 1 (<100 tenants) comme documenté dans le code. À optimiser en ajoutant `schema_name` directement en colonne dans la requête principale + stocker en champ temporaire (ou via un `Map<UUID, String>` chargé au préalable pour la page entière). Fichier : `AdminTenantService.java#enrichWithCrossSchemaMetrics`.

### Migré depuis: bugfix spec-hf2-ghost-badge-sales-scoping (2026-04-28)

- **D1 — Pas de test pour le path EMPLOYEE scopé**: `todaySummaryProvider` avec `employeeId != null` n'a aucune couverture de test. Les tests adaptés (`day_close_button_test.dart`, `day_summary_bottom_sheet_test.dart`) n'exercent que le chemin OWNER (`employeeId: null`). Ajouter un test widget où `currentUserRoleProvider` retourne EMPLOYEE + `currentUserIdProvider` retourne un userId, et vérifier que `todaySummaryProvider((storeId: X, employeeId: userId))` est bien invoqué.
- **D2 — `currentUserIdProvider` état d'erreur → fallback silencieux store-wide**: Si le storage est indisponible et `currentUserIdProvider` retourne `AsyncError`, `userIdAsync.valueOrNull` retourne `null` → un EMPLOYEE voit tout le magasin sans erreur UI. À adresser lorsqu'une politique globale de gestion d'erreurs provider sera définie.
- **D3 — Null semantics de `computeTodaySummary` non documentés côté repository**: Le contrat de `null` comme `employeeId` (= "toutes les ventes") n'est documenté qu'au niveau du provider Riverpod. Si `computeTodaySummary(storeId, null)` est appelé directement depuis un background job ou un usecase futur, l'élevation de visibilité est silencieuse. Documenter dans la doc du repository ou dans le domain layer.

### Migré depuis: code review of HF-1-registration-role-persistence-unified-auth-ui (2026-04-19)

- **D1 — CGU links non-tappables**: `TextSpan`s "Conditions d'utilisation" et "Politique de confidentialité" dans `auth_page.dart` ont une décoration underline+couleur mais aucun `GestureRecognizer`. Comportement hérité de l'ancien `RegisterPage`. Ajouter un `TapGestureRecognizer` vers `/docs/politique` lors d'une itération UI dédiée.
- **D2 — `kUserRoleKey = 'OWNER'` hardcodé sans lecture du serveur**: `auth_provider.dart` Registration.register() écrit `'OWNER'` en dur au lieu de lire le rôle depuis le `RegistrationResult`. La spec l'autorise explicitement ("hardcoded, since registration always creates an OWNER") mais en cas d'évolution du backend (multi-role onboarding), cela causera un privilege assignment silencieux côté client.
- **D3 — `prefs.setString()` non awaité**: Dans `auth_provider.dart` (Registration et Login) et `auth_page.dart`, les écritures SharedPreferences sont fire-and-forget. Un échec disque est silencieux. À corriger globalement lorsqu'une politique d'erreur persistence sera définie.
- **D4 — Pattern routing `role == 'OWNER' ? '/dashboard' : '/pos'` dupliqué**: Ce ternaire apparaît en 4 endroits (`app_router.dart`, `auth_page.dart`, `shop_name_page.dart` ×2). Extraire en helper `routeForRole(String? role) → String` lors du prochain refactor routing.
- **D5 — Router redirect autorise `role == null` sur routes OWNER-only**: le redirect `app_router.dart` vérifie `role == 'EMPLOYEE'` pour rediriger vers `/pos`, mais laisse passer un rôle null silence vers les routes owner. Pre-existing. Ajouter `if (role == null) return '/auth/login';` dans le guard.
- **D6 — Back en mode login sans historique → `/auth/register`**: comportement hérité de l'ancien `LoginPage`. Peut-être contre-intuitif pour les utilisateurs deep-link. À revoir dans une itération navigation.
- **D7 — `initialCountryCode: 'CM'` hardcodé**: `auth_page.dart` (et anciens pages) forcent le Cameroun sans détection de locale. Ajouter détection via `flutter_sim_locale` ou prefs dans une itération internationalisation.

### Migré depuis: code review of 8-0-fcm-push-notifications-whatsapp-wassender (2026-04-03)

- `DeviceTokenController.deleteToken()` — pas de vérification de propriété (ownership) sur la suppression de token. Tout utilisateur authentifié connaissant un token peut le supprimer. Surface d'attaque faible (tokens opaques, ~512 chars) mais représente un pattern à améliorer quand une liste par user_id sera disponible.

### Migré depuis: code review of 7-5-configuration-des-rapports-preferences-whatsapp (2026-04-02)

- `WhatsAppPort.isConfigured()` has a `default return true` — any new adapter that forgets to override would silently send real test reports. Consider making the method abstract, or at minimum adding a code smell lint rule. Pre-existing design decision; not caused by Story 7.5 changes.

### Migré depuis: cart UI session (2026-04-07) — entrée stale sync_queue

- **Stale sync_queue payloads with `'status'` key**: Any `CREATE_SALE` operations queued before the `_buildPayload()` key rename (`'status'` → `'requestedStatus'`) will sync as COMPLETED (not PENDING_VALIDATION). These are pre-existing broken payloads — the rename doesn't make them worse, but they should be cleared manually if observed in production. A future migration step could detect and fix `sync_queue` entries that contain `"status"` instead of `"requestedStatus"`.
- **Note v1s-15-5** : l'entrée "Ventes brouillon en mode offline" du même bloc legacy n'est PAS migrée — obsolète depuis le pivot v1s-13-6 (commit `30bdd07`, 2026-05-03) qui a remplacé le flux brouillon par saisie de stock initial inline + auto-promotion.

### Migré depuis: code review of 8-1-alertes-stock-critique-tendances-de-ventes (2026-04-04)

- **D1 — `ConcurrentHashMap<BatchWindow>` never evicted** — `StockAlertNotificationListener.batchWindows` (et `StockModificationNotificationListener`, `ProductCreationNotificationListener`) accumule une entrée par `tenantId:storeId` (ou `tenantId:actorId`). Growth bounded by tenant×store count (~40 bytes each). Consider Caffeine cache with TTL or periodic cleanup post-MVP.
- **D2 — TOCTOU race between cooldown check and upsert** — Two concurrent `@Async` threads for the same product+store could both pass `existsActiveCooldown` and both dispatch. Narrow window, benign duplicate. Consider DB-level advisory lock if notification dedup becomes critical.
- **D3 — No `@SchedulerLock` for overlapping trend scheduler runs** — If a `detectTrends()` run exceeds 1 hour, the next cron invocation starts concurrently. Consider integrating ShedLock when scaling to many tenants.

### Migré depuis: code review of 5-6-offline-first-transactional-writes-instant-ux (2026-04-23)

- **F6 — `RiverpodSyncTriggerDispatcher` captures `Ref` at construction** — If the Provider scope is recreated (test overrides or auth scope changes), the captured `_ref` may become stale. Pre-existing pattern across the project; negligible in production. Revisit when Provider scoping is formalized.

---

## Nouveaux defers créés par v1s-15-5 (2026-07-23)

### D2 — Gap audit event Category (toggleActive / createCustom)

- **Source** : `JpaCategoryRepository.java:79,96` (lignes après suppression TODOs) — les 2 TODOs `// TODO: emit audit event for category toggle / custom category creation` ont été retirés du code dans v1s-15-5 et convertis en cette entrée defer. Les appels `jpaRepository.save()` sont aux lignes 79 (`toggleActive`) et 96 (`createCustom`) ; l'émission d'événement d'audit devrait se situer entre `save()` et `return toDomain()`.
- **Gap** : les mutations `toggleActive` et `createCustom` de Category ne sont pas auditées, contrairement à la majorité des écritures métier du système (Story 1.8). L'émission d'un domain event + listener d'audit est une nouvelle fonctionnalité, hors scope d'une story de nettoyage.
- **Cross-référence** : même voisinage de code que le D2 de `v1s-15-3-red-tests-missing-classes.md` (3 use cases Category lèvent `IllegalArgumentException` non catchée → 500). Les deux gaps (audit manquant + 500 sur erreur) pourront être traités dans une même story de suivi Category.
- **Fichiers** : `JpaCategoryRepository.java`, `CategoryRepository.java`.

### D3 — Gap DB-level enforcement audit log (trigger/REVOKE)

- **Source** : réconciliation `sprint-status.yaml` `11-3-audit-append-only-db` (v1s-15-5 AC5).
- **Gap** : l'enforcement applicatif de l'immutabilité de `audit_log` est complet (port sans update/delete, entité `@Immutable`, guards 403 `AUDIT_IMMUTABLE`, tests) — livré par Story 1.8. Mais aucune protection niveau base de données n'existe : pas de trigger `BEFORE UPDATE/DELETE`, pas de `REVOKE UPDATE, DELETE`, pas de RLS. L'immutabilité actuelle protège contre le code applicatif normal, pas contre une requête SQL directe ou un futur bug ORM.
- **Fichiers** : `V1__baseline_public.sql:22-30` (définit `audit_log` comme table standard), `AuditPort.java`, `AuditLogSpringRepository.java`, `AuditLogJpaEntity.java`, `AuditController.java`.
- **Ne pas corriger ici** — requires une migration Flyway (nouveau code de production, hors scope hygiène).
